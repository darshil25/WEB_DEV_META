//in ES5 Version of JS we can built strings using('') and ("") 
//But in ES6 version new way introduce to make string using (``)

//ES5
let multiline = "No-multiline string in ES5";
console.log("Did you know ? " + multiline);

multiline = `
Using ES6
backticks,
mult-line
strings are
possible!`;
    console.log("Did you know ? " + multiline);